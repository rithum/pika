---
import CounterSvelte from './counter.svelte';

interface Props {
	clientLoad?: boolean;
}

const { clientLoad = false } = Astro.props;
---

{clientLoad ? (
	<CounterSvelte client:load />
) : (
	<CounterSvelte />
)}

