// Jest setup file for global test configuration

// You can add global test setup here if needed
// For example, mock implementations that should apply to all tests

// Mock console methods if needed to reduce noise during tests
// const originalConsole = console;
// global.console = {
//   ...originalConsole,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };

// Add any global mocks or setup here
export {};
